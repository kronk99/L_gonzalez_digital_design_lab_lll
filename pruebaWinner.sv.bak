module pruebaWinner(input logic clk,rst ,output);
	winner winner_inst (
    .clk(clk),                           // Entrada: Reloj
    .id(id),                             // Entrada: Identificador
    .enable(enable),                     // Entrada: Habilitador
    .matrix_player(matrix_player),       // Salida: Matriz del jugador
    .matrix_pc(matrix_pc)                // Salida: Matriz de la PC
);

endmodule 